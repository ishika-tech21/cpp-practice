#include <iostream>

// Using explicit scoping instead of 'using namespace std;' is preferred in production 
using std::cout;
using std::endl;

int main(){
    //Fast I/O for performance optimize
    std::ios_base::sync_with_studio(false);
    std::cin.tie(NULL);
    
    cout<<"Hello, Github! Repository initialized successfully."<<endl;
    
    return 0;
}

